public class EmailClient {
    public static void main(String[] args) {
	SecurityEntry securityEntry = new SecurityEntry();
	EmailClientGUI gui = new EmailClientGUI(); /*
	if (Globals.entryGranted) {
	    int error = MailTransfers.EmailClientRequestAllMail(Globals.RECEIVER_ID);
	    if (error == Globals.PROCESS_OK) { // from Part 7
		EmailClientGUI gui = new EmailClientGUI();
	    }
	} */
    }
}
